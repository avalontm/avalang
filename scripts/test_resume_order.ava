func gen()
    yield 42
end

co = coroutine(gen)
print("Coroutine type:", type(co))

res = resume(co)
print("Resume result:", res)
print("Resume type:", type(res))

zero = 0
print("Result[0]:", res[zero])
print("Result[0] type:", type(res[zero]))
