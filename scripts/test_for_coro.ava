func counter()
    i = 0
    while i < 5 do
        yield i
        i = i + 1
    end
end

print("Testing for-in with generator:")
for n in coroutine(counter) do
    print("  Got: " + str(n))
    if n >= 3 then
        break
    end
end
print("Done!")
