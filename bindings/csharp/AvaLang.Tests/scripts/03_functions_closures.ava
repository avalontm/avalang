# Funciones, recursividad, closures.
func factorial(n):
    if n <= 1:
        return 1
    return n * factorial(n - 1)

func make_adder(x):
    return func(y): x + y

add5 = make_adder(5)

factorial(5) + add5(10)
